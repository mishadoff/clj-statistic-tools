#include<iostream>
#include<cstdio>
#include<cstdlib>
#include<memory.h>
#include<cmath>
#include<ctime>
#include<algorithm>
#include<string>
#include<map>
#include<set>
#define abs(x) ((x)<0?-(x):(x))
#define min(a,b) ((a)<(b)?(a):(b))
#define max(a,b) ((a)>(b)?(a):(b))
#define sqr(x) ((x)*(x))
#define getar(m,n) for(int _=0;_<n;++_) cin>>(m)[_];
#define foreach(it,c) for(typeof((c).begin()) it=(c).begin(); it!=(c).end(); it++)
#define fill(m,v) memset(m,v,sizeof(m))
#define random(x) (((rand()<<15)+rand())%(x))
#define y1 stupid_cmath
#define y0 stupid_cmath_make_me_cry
#define tm stupid_ctime
#define long long long
using namespace std;

int n,m;
int i,j,k,l;
char s[1111];
int c[333];
pair<int,char> ab[111];

int main(){
	freopen("input.txt","r",stdin); freopen("output.txt","w",stdout);
	
	scanf("%d",&m);
	gets(s);
	for(k=1;k<=m;++k){
		gets(s);
		n=strlen(s);
		fill(c,0);
		for(i=0;i<n;++i){
			if(s[i]>='A'&&s[i]<='Z') s[i]=s[i]-'A'+'a';
			if(s[i]>='a'&&s[i]<='z') c[s[i]]++;
		}
		l=0;
		for(i='a';i<='z';++i) if(c[i]) ab[l++]=make_pair(c[i],i);
		sort(ab,ab+l);
		reverse(ab,ab+l);
		j=26;
		fill(c,0);
		for(i=0;i<l;++i){
			if(!c[ab[i].second]) c[ab[i].second] = j--;
		}
		l=0;
		for(i=0;s[i];++i){
				if(s[i]>='a'&&s[i]<='z') l+=c[s[i]];
			
		}
		printf("Case #%d: %d\n",k,l);
	}
	
	return 0;
}
