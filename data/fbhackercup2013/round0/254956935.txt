import sys

N = int(sys.stdin.readline())

for case in xrange(1,N+1):
	s = sys.stdin.readline().lower()
	cnt = [ s.count(chr(97+i)) for i in xrange(26) ]
	cnt.sort()
	r = sum( x*y for (x,y) in zip(cnt,range(1,27)) )
	print "Case #%d: %d"% (case, r)
