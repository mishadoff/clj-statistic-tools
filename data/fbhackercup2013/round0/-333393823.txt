#include <cstdio>
#include <iostream>
#include <sstream>
#include <fstream>
#include <iomanip>
#include <algorithm>
#include <cmath>
#include <string>
#include <vector>
#include <list>
#include <queue>
#include <stack>
#include <set>
#include <map>
#include <bitset>
#include <numeric>
#include <climits>
#include <cfloat>
using namespace std;

int main()
{
    int t;
    cin >> t;

    for(int x=1; x<=t; ++x){
        int n, k, a, b, c, r;
        cin >> n >> k >> a >> b >> c >> r;

        vector<int> v(k+1);
        v[0] = INT_MAX;
        v[1] = a;
        for(int i=2; i<=k; ++i)
            v[i] = (b * (long long)v[i-1] + c) % r;

        vector<bool> used(k+2, false);
        for(int i=k; i>=1; --i){
            if(v[i] <= k){
                if(!used[v[i]])
                    used[v[i]] = true;
                else
                    v[i] = INT_MAX;
            }
        }

        int j = 0;
        for(int i=0; i<=k; ++i){
            while(used[j])
                ++ j;

            if(v[i] > k){
                v[i] = j;
                used[j] = true;
            }else if(v[i] > j){
                used[v[i]] = false;
                v[i] = j;
                used[j] = true;
            }
        }

        int ret = v[n%(k+1)];
        cout << "Case #" << x << ": " << ret << endl;
    }

    return 0;
}