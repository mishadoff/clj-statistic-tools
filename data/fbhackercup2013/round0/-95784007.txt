import sys
import string

for i, line in enumerate(sys.stdin):
  if i == 0: continue
  line = line.lower()
  stat = [line.count(c) for c in string.lowercase]
  stat.sort()

  print 'Case #%d:' % i, sum((k+1)*v for k,v in enumerate(stat))
